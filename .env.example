# Hugging Face token for BGE embeddings (BAAI/bge-base-en-v1.5) via the Inference API.
# Free token: https://huggingface.co/settings/tokens (read access is enough)
HF_TOKEN=

# LLM provider key (used by the agent graph, not by retrieval.py)
OPENAI_API_KEY=
